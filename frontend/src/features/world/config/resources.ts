import type { ResourceType } from "../types/world";

export const RESOURCE_LABELS: Record<
    ResourceType,
    string
> = {
    FOOD: "Nourriture",
    WOOD: "Bois",
    STONE: "Pierre",
    ORE: "Minerai",
};